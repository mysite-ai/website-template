---
import type { TenantContext } from "@/lib/tenant/types";
import { ArrowUpRight } from "lucide-react";

interface Props {
  tenant: TenantContext;
}

const { tenant } = Astro.props;
const delivery = tenant.location.delivery;
---

{delivery.length > 0 && (
  <section class="section-well mt-10">
    <p class="section-label mb-3 px-1">Delivery</p>
    <div class="card overflow-hidden">
      <ul class="divide-y divide-border/40">
        {delivery.map((link) => (
          <li>
            <a
              href={link.url}
              target="_blank"
              rel="noopener noreferrer"
              class="flex items-center justify-between px-5 py-3.5 sm:px-6 text-[14px] font-medium hover:bg-muted/40 transition-colors"
              data-umami-event="click-delivery-provider"
              data-umami-event-target={link.name}
            >
              <span>{link.name}</span>
              <ArrowUpRight size={15} strokeWidth={2} className="text-muted-foreground" aria-hidden="true" />
            </a>
          </li>
        ))}
      </ul>
    </div>
  </section>
)}
