---
import type { TenantContext } from "@/lib/tenant/types";
import GalleryBrowser from "./GalleryBrowser.tsx";

interface Props {
  tenant: TenantContext;
}

const { tenant } = Astro.props;
const gallery = tenant.location.gallery.slice(0, 9);
---

{gallery.length > 0 && (
  <section class="section-well mt-6">
    <p class="section-label mb-3 px-1">Gallery</p>
    <GalleryBrowser client:visible images={gallery} />
  </section>
)}
